bind_addr = "0.0.0.0"
bind_port = 17000

log_level = "info"

[auth]
method = "token"
token = "replace_with_token"

[transport]
protocol = "tcp"
tcp_mux = true
pool_count = 2

[[allow_ports]]
start = 6000
end = 7000
